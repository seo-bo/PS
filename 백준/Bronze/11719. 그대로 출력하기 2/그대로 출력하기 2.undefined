#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main(void)
{
	cin.tie(0)->sync_with_stdio(0);
	string str;
	while (getline(cin, str))
	{
		cout << str << '\n';
	}
	return 0;
}