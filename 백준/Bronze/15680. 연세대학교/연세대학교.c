#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main(void)
{
	int num = 0;
	scanf("%d", &num);
	if (num == 0)
	{
		printf("YONSEI");
	}
	else if(num == 1)
	{
		printf("Leading the Way to the Future");
	}
}