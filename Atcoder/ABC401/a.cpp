#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main(void)
{
	cin.tie(0)->sync_with_stdio(0);
	int n = 0;
	cin >> n;
	if (n >= 200 && n <= 299)
	{
		cout << "Success";
		return 0;
	}
	cout << "Failure";
	return 0;
}
